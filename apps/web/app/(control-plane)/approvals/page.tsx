import type { Metadata } from "next"; import { approvalDisplayStateSchema } from "@docs-agent/control-plane"; import { ApprovalInbox } from "@/components/approval-inbox"; import { PageHeading } from "@/components/page-heading"; import { resolveApprovalList, type ApprovalFilters } from "@/lib/approval-review";
export const metadata: Metadata = { title: "Approval inbox" }; export const dynamic = "force-dynamic";
export default async function Page({ searchParams }: { searchParams: Promise<Record<string, string | string[] | undefined>> }) { const params = await searchParams; const state = approvalDisplayStateSchema.safeParse(single(params.state)); const query = single(params.query)?.trim().slice(0, 200); const filters: ApprovalFilters = { state: state.success ? state.data : undefined, query: query || undefined }; const result = await resolveApprovalList(filters, single(params.scenario)); return <div className="grid gap-[clamp(2rem,6vw,5rem)]"><PageHeading index="08" title="Approval inbox" summary="Find pending side effects across signals and channels, review their evidence, and resume the original Eve request once." /><ApprovalInbox filters={filters} result={result} /></div>; } function single(value: string | string[] | undefined) { return Array.isArray(value) ? value[0] : value; }
