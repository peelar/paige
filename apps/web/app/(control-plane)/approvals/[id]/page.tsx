import type { Metadata } from "next"; import { ApprovalDetail } from "@/components/approval-detail"; import { PageHeading } from "@/components/page-heading"; import { resolveApprovalDetail } from "@/lib/approval-review";
export const metadata: Metadata = { title: "Approval request" }; export const dynamic = "force-dynamic";
export default async function Page({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<{ scenario?: string }> }) { const [{ id }, { scenario }] = await Promise.all([params, searchParams]); const result = await resolveApprovalDetail(id, scenario); return <div className="grid gap-[clamp(2rem,6vw,5rem)]"><PageHeading index="08 / detail" title="Approval detail" summary="Review the proposed side effect and answer Eve’s durable input request without recreating its execution state." /><ApprovalDetail result={result} /></div>; }
