{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".eve/**", ".next/**", "!.next/cache/**", ".output/**", "dist/**"]
    },
    "paige#build": {
      "dependsOn": ["^build"],
      "env": ["PAIGE_SLACK_CONNECTOR", "EVE_GATEWAY_MODEL"],
      "outputs": [".eve/**", ".vercel/output/**", ".output/**"]
    },
    "check": {
      "outputs": []
    },
    "dev": {
      "cache": false,
      "env": [
        "PAIGE_DATABASE_AUTH_TOKEN",
        "PAIGE_DATABASE_URL",
        "PAIGE_OPERATOR_ACCESS"
      ],
      "persistent": true
    },
    "dev:portless": {
      "cache": false,
      "env": [
        "PAIGE_DATABASE_AUTH_TOKEN",
        "PAIGE_DATABASE_URL",
        "PAIGE_OPERATOR_ACCESS"
      ],
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
